const fs = require("fs");

/*
  ------------------------
  SOLUTION 2
  ------------------------
*/

const calculateSolution2 = async () => {
  const data = await fs.readFileAsync("input.txt", "UTF8");
};

module.exports = {
  calculateSolution2
};
